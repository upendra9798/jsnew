//if,if else 

if (2 == "3") {
    console.log("executed"); //Work (not check datatype)
    }
    if (2==="2") {
        console.log(exected);  // not work (also checks datatype)
        
    }

    //var can work outside scope so avoid its use